# Make sure .env is not committed
#In this folder, we have 2 scripts: 
	#One for demostrating naive extraction of document content in a RAG pipeline
	#Second one does improved extraction, where it takes care of tables, multiple columns, and size of each sections
# Script can be executed using below command:

python <script name>